{
  "name": "blockcerts-universal-verifier",
  "description": "A standalone universal viewer &amp; verifier for blockcerts credentials",
  "main": "blockcerts-universal-verifier.js",
  "dependencies": {
    "@polymer/lit-element": "^0.5.1"
  },
  "devDependencies": {
    "@polymer/iron-demo-helpers": "^3.0.0-pre.19",
    "@webcomponents/webcomponentsjs": "^2.0.0",
    "axe-core": "^3.0.2",
    "husky": "^0.14.3",
    "node-sass": "^4.9.0",
    "polymer-cli": "^1.7.2",
    "pwa-helpers": "^0.8.2",
    "semistandard": "^12.0.1",
    "snazzy": "^7.1.1",
    "wct-browser-legacy": "^1.0.1",
    "web-component-tester": "^6.6.0"
  },
  "scripts": {
    "test": "polymer test",
    "start": "polymer serve",
    "styles:compile": "node scripts/styles/compile-styles.js",
    "lint": "npm run lint:js && npm run lint:polymer",
    "lint:js": "semistandard \"**/*.js\" | snazzy",
    "lint:polymer": "polymer lint",
    "prepush": "npm run test && npm run lint"
  }
}
